
  # Instagram Feed Planner

  This is a code bundle for Instagram Feed Planner. The original project is available at https://www.figma.com/design/MrMaL3ke44Kw45dsleH0dD/Instagram-Feed-Planner.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  